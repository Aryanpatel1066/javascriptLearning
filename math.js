 // math.js
export const add = (a, b) => a + b;
export const sub = (x, y) => x - y;
export const mul = (p, q) => p * q;

const defaultExport = 'I am the default export';
export default defaultExport;
